version: '2'

run:
  timeout: 5m
  tests: false
  relative-path-mode: gomod

linters:
  default: none
  enable:
    - revive
  settings:
    revive:
      enable-all-rules: false
      rules:
        - name: exported

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
